import 'package:flutter/material.dart';

class ProductosList extends StatelessWidget {
  const ProductosList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Listado productos"),
    );
  }
}